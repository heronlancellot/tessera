# Configuração do npm para pacotes privados
# Para usar npm privado, descomente e configure:
# @tessera:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# Para usar npm público com escopo privado:
# @tessera:registry=https://registry.npmjs.org/

# Configuração padrão (npm público)
registry=https://registry.npmjs.org/
